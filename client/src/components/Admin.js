const Admin = () => (
    <section aria-labelledby="admin-title">
      <h2 id="admin-title">Panel de Administración</h2>
      <p>Solo los usuarios registrados puede ver este panel.</p>
    </section>
  );
  
  export default Admin;